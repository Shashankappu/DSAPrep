package com.dsamaster.app.data

import com.dsamaster.app.data.model.Example
import com.dsamaster.app.data.model.Problem

fun getProblems2(): List<Problem> = listOf(
        Problem(
            id = 31,
            title = "Median of Two Sorted Arrays",
            category = "Binary Search",
            difficulty = "Hard",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE", "Google Fav"),
            tags = listOf("Binary Search", "Divide and Conquer"),
            description = "Find median of two sorted arrays in O(log(m+n)) time.",
            examples = listOf(
                Example("nums1=[1,3], nums2=[2]", "2.0", ""),
                Example("nums1=[1,2], nums2=[3,4]", "2.5", ""),
            ),
            constraints = listOf("0 <= m, n <= 1000", "1 <= m + n <= 2000"),
            explanation = "Binary search on partition of smaller array. Find partition where all left elements <= all right elements.",
            approach = "Binary search on smaller array's partition. Check valid partition. Calculate median from boundary.",
            code = "class Solution {\npublic:\n    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {\n        if (nums1.size() > nums2.size()) swap(nums1, nums2);\n        int m=nums1.size(), n=nums2.size(), l=0, r=m;\n        while (l <= r) {\n            int i=(l+r)/2, j=(m+n+1)/2-i;\n            int left1 = i==0?INT_MIN:nums1[i-1];\n            int right1 = i==m?INT_MAX:nums1[i];\n            int left2 = j==0?INT_MIN:nums2[j-1];\n            int right2 = j==n?INT_MAX:nums2[j];\n            if (left1<=right2 && left2<=right1) {\n                if ((m+n)%2==0) return (max(left1,left2)+min(right1,right2))/2.0;\n                return max(left1,left2);\n            } else if (left1>right2) r=i-1;\n            else l=i+1;\n        }\n        return 0;\n    }\n};",
            timeComplexity = "O(log(min(m,n)))",
            spaceComplexity = "O(1)",
            videoUrl = "https://www.youtube.com/watch?v=q6IEA26hvXc"
        ),
        Problem(
            id = 32,
            title = "Reverse Linked List",
            category = "Linked List",
            difficulty = "Easy",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE"),
            tags = listOf("Linked List"),
            description = "Reverse a singly linked list and return the reversed list.",
            examples = listOf(
                Example("head = [1,2,3,4,5]", "[5,4,3,2,1]", ""),
            ),
            constraints = listOf("0 <= number of nodes <= 5000"),
            explanation = "Use three pointers: prev, curr, next. At each step, reverse curr's pointer to prev, then advance.",
            approach = "prev=null, curr=head. Save next, point curr->next to prev, advance prev and curr.",
            code = "class Solution {\npublic:\n    ListNode* reverseList(ListNode* head) {\n        ListNode* prev = nullptr;\n        ListNode* curr = head;\n        while (curr) {\n            ListNode* next = curr->next;\n            curr->next = prev;\n            prev = curr;\n            curr = next;\n        }\n        return prev;\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(1)",
            videoUrl = "https://www.youtube.com/watch?v=G0_I-ZF0S38"
        ),
        Problem(
            id = 33,
            title = "Merge Two Sorted Lists",
            category = "Linked List",
            difficulty = "Easy",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE"),
            tags = listOf("Linked List"),
            description = "Merge two sorted linked lists into one sorted list.",
            examples = listOf(
                Example("list1=[1,2,4], list2=[1,3,4]", "[1,1,2,3,4,4]", ""),
            ),
            constraints = listOf("0 <= nodes <= 50"),
            explanation = "Use dummy node, compare heads of both lists, attach smaller node to result.",
            approach = "Dummy head, tail pointer. Compare l1 and l2 values, attach smaller. Append remaining.",
            code = "class Solution {\npublic:\n    ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) {\n        ListNode dummy(0);\n        ListNode* tail = &dummy;\n        while (l1 && l2) {\n            if (l1->val <= l2->val) { tail->next=l1; l1=l1->next; }\n            else { tail->next=l2; l2=l2->next; }\n            tail = tail->next;\n        }\n        tail->next = l1 ? l1 : l2;\n        return dummy.next;\n    }\n};",
            timeComplexity = "O(n+m)",
            spaceComplexity = "O(1)",
            videoUrl = "https://www.youtube.com/watch?v=XIdigk956u0"
        ),
        Problem(
            id = 34,
            title = "Linked List Cycle",
            category = "Linked List",
            difficulty = "Easy",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE"),
            tags = listOf("Linked List", "Two Pointers"),
            description = "Determine if a linked list has a cycle using Floyd's algorithm.",
            examples = listOf(
                Example("head = [3,2,0,-4], pos = 1", "true", ""),
            ),
            constraints = listOf("0 <= nodes <= 10^4"),
            explanation = "Floyd's Tortoise and Hare: slow moves 1 step, fast moves 2 steps. If they meet, there's a cycle.",
            approach = "slow=head, fast=head. Move slow by 1, fast by 2. If they meet, cycle exists.",
            code = "class Solution {\npublic:\n    bool hasCycle(ListNode* head) {\n        ListNode *slow=head, *fast=head;\n        while (fast && fast->next) {\n            slow = slow->next;\n            fast = fast->next->next;\n            if (slow == fast) return true;\n        }\n        return false;\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(1)",
            videoUrl = "https://www.youtube.com/watch?v=gBTe7lFR3vc"
        ),
        Problem(
            id = 35,
            title = "Reorder List",
            category = "Linked List",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150"),
            tags = listOf("Linked List", "Two Pointers"),
            description = "Reorder list from L0->L1->...->Ln to L0->Ln->L1->Ln-1->...",
            examples = listOf(
                Example("head = [1,2,3,4]", "[1,4,2,3]", ""),
            ),
            constraints = listOf("1 <= nodes <= 5*10^4"),
            explanation = "Find middle with slow/fast. Reverse second half. Merge both halves alternately.",
            approach = "1) Find mid. 2) Reverse second half. 3) Merge first and reversed second half.",
            code = "class Solution {\npublic:\n    void reorderList(ListNode* head) {\n        if (!head || !head->next) return;\n        ListNode *slow=head, *fast=head->next;\n        while (fast && fast->next) { slow=slow->next; fast=fast->next->next; }\n        ListNode* second = slow->next;\n        slow->next = nullptr;\n        ListNode* prev = nullptr;\n        while (second) {\n            ListNode* next=second->next;\n            second->next=prev; prev=second; second=next;\n        }\n        ListNode *first=head; second=prev;\n        while (second) {\n            ListNode *t1=first->next, *t2=second->next;\n            first->next=second; second->next=t1;\n            first=t1; second=t2;\n        }\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(1)",
            videoUrl = "https://www.youtube.com/watch?v=S5bfdUTrKLM"
        ),
        Problem(
            id = 36,
            title = "Remove Nth Node From End",
            category = "Linked List",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE"),
            tags = listOf("Linked List", "Two Pointers"),
            description = "Remove the nth node from the end of the list.",
            examples = listOf(
                Example("head = [1,2,3,4,5], n = 2", "[1,2,3,5]", ""),
            ),
            constraints = listOf("1 <= sz <= 30", "1 <= n <= sz"),
            explanation = "Two pointers n apart. When fast reaches end, slow is before the target node.",
            approach = "Dummy head. Advance fast n+1 steps. Move both until fast is null. Delete slow->next.",
            code = "class Solution {\npublic:\n    ListNode* removeNthFromEnd(ListNode* head, int n) {\n        ListNode dummy(0, head);\n        ListNode *slow=&dummy, *fast=&dummy;\n        for (int i=0;i<=n;i++) fast=fast->next;\n        while (fast) { slow=slow->next; fast=fast->next; }\n        slow->next = slow->next->next;\n        return dummy.next;\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(1)",
            videoUrl = "https://www.youtube.com/watch?v=XVuQxVej6y8"
        ),
        Problem(
            id = 37,
            title = "Merge K Sorted Lists",
            category = "Linked List",
            difficulty = "Hard",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE", "Google Fav"),
            tags = listOf("Linked List", "Heap"),
            description = "Merge k sorted linked lists into one sorted list.",
            examples = listOf(
                Example("lists = [[1,4,5],[1,3,4],[2,6]]", "[1,1,2,3,4,4,5,6]", ""),
            ),
            constraints = listOf("0 <= k <= 10^4"),
            explanation = "Use min-heap storing heads of all lists. Pop smallest, add to result, push its next node.",
            approach = "Min-heap with comparator. Push all heads. Pop min, attach to result, push next.",
            code = "class Solution {\npublic:\n    ListNode* mergeKLists(vector<ListNode*>& lists) {\n        auto cmp=[](ListNode* a,ListNode* b){return a->val>b->val;};\n        priority_queue<ListNode*,vector<ListNode*>,decltype(cmp)> pq(cmp);\n        for (auto* l : lists) if (l) pq.push(l);\n        ListNode dummy(0);\n        ListNode* tail = &dummy;\n        while (!pq.empty()) {\n            ListNode* node=pq.top(); pq.pop();\n            tail->next=node; tail=tail->next;\n            if (node->next) pq.push(node->next);\n        }\n        return dummy.next;\n    }\n};",
            timeComplexity = "O(n log k)",
            spaceComplexity = "O(k)",
            videoUrl = "https://www.youtube.com/watch?v=q5a5OiGbT6Q"
        ),
        Problem(
            id = 38,
            title = "Invert Binary Tree",
            category = "Trees",
            difficulty = "Easy",
            sources = listOf("Blind 75", "Neetcode 150"),
            tags = listOf("Tree", "DFS", "Recursion"),
            description = "Invert a binary tree and return its root.",
            examples = listOf(
                Example("root = [4,2,7,1,3,6,9]", "[4,7,2,9,6,3,1]", ""),
            ),
            constraints = listOf("0 <= nodes <= 100"),
            explanation = "Recursively swap left and right children of every node.",
            approach = "If root null, return. Swap children. Recurse on both subtrees.",
            code = "class Solution {\npublic:\n    TreeNode* invertTree(TreeNode* root) {\n        if (!root) return nullptr;\n        swap(root->left, root->right);\n        invertTree(root->left);\n        invertTree(root->right);\n        return root;\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(h)",
            videoUrl = "https://www.youtube.com/watch?v=OnSn2XEQ4MY"
        ),
        Problem(
            id = 39,
            title = "Maximum Depth of Binary Tree",
            category = "Trees",
            difficulty = "Easy",
            sources = listOf("Blind 75", "Neetcode 150"),
            tags = listOf("Tree", "DFS", "BFS"),
            description = "Return maximum depth (longest root-to-leaf path) of a binary tree.",
            examples = listOf(
                Example("root = [3,9,20,null,null,15,7]", "3", ""),
            ),
            constraints = listOf("0 <= nodes <= 10^4"),
            explanation = "Recursively find max depth of left and right subtrees. Add 1 for current node.",
            approach = "Base: null returns 0. Return 1 + max(depth(left), depth(right)).",
            code = "class Solution {\npublic:\n    int maxDepth(TreeNode* root) {\n        if (!root) return 0;\n        return 1 + max(maxDepth(root->left), maxDepth(root->right));\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(h)",
            videoUrl = "https://www.youtube.com/watch?v=hTM3phVI6YQ"
        ),
        Problem(
            id = 40,
            title = "Same Tree",
            category = "Trees",
            difficulty = "Easy",
            sources = listOf("Blind 75", "Neetcode 150"),
            tags = listOf("Tree", "DFS"),
            description = "Check if two binary trees are structurally identical with same values.",
            examples = listOf(
                Example("p=[1,2,3], q=[1,2,3]", "true", ""),
            ),
            constraints = listOf("0 <= nodes <= 100"),
            explanation = "Recursively check: both null (true), one null (false), values differ (false), compare subtrees.",
            approach = "If both null, true. If one null or vals differ, false. Recurse left and right.",
            code = "class Solution {\npublic:\n    bool isSameTree(TreeNode* p, TreeNode* q) {\n        if (!p && !q) return true;\n        if (!p || !q || p->val != q->val) return false;\n        return isSameTree(p->left,q->left) && isSameTree(p->right,q->right);\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(h)",
            videoUrl = "https://www.youtube.com/watch?v=vRbbcKXCxOw"
        ),
        Problem(
            id = 41,
            title = "Subtree of Another Tree",
            category = "Trees",
            difficulty = "Easy",
            sources = listOf("Blind 75", "Neetcode 150"),
            tags = listOf("Tree", "DFS"),
            description = "Check if subRoot is a subtree of root.",
            examples = listOf(
                Example("root=[3,4,5,1,2], subRoot=[4,1,2]", "true", ""),
            ),
            constraints = listOf("1 <= nodes <= 2000"),
            explanation = "For each node in root, check if the subtree there is identical to subRoot.",
            approach = "If root null, false. Check if current matches subRoot. Else recurse on left and right.",
            code = "class Solution {\npublic:\n    bool isSubtree(TreeNode* root, TreeNode* subRoot) {\n        if (!root) return false;\n        if (isSame(root, subRoot)) return true;\n        return isSubtree(root->left,subRoot) || isSubtree(root->right,subRoot);\n    }\n    bool isSame(TreeNode* p, TreeNode* q) {\n        if (!p&&!q) return true;\n        if (!p||!q||p->val!=q->val) return false;\n        return isSame(p->left,q->left) && isSame(p->right,q->right);\n    }\n};",
            timeComplexity = "O(m*n)",
            spaceComplexity = "O(h)",
            videoUrl = "https://www.youtube.com/watch?v=E36O5SWp-LE"
        ),
        Problem(
            id = 42,
            title = "Lowest Common Ancestor of BST",
            category = "Trees",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE"),
            tags = listOf("BST"),
            description = "Find the lowest common ancestor of two nodes in a BST.",
            examples = listOf(
                Example("root=[6,2,8,0,4,7,9], p=2, q=8", "6", ""),
            ),
            constraints = listOf("All values unique", "p != q", "Both exist"),
            explanation = "In BST, if both < root go left, if both > root go right, else current is LCA.",
            approach = "If both p,q < root, left. If both > root, right. Otherwise current is LCA.",
            code = "class Solution {\npublic:\n    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {\n        while (root) {\n            if (p->val < root->val && q->val < root->val) root=root->left;\n            else if (p->val > root->val && q->val > root->val) root=root->right;\n            else return root;\n        }\n        return nullptr;\n    }\n};",
            timeComplexity = "O(h)",
            spaceComplexity = "O(1)",
            videoUrl = "https://www.youtube.com/watch?v=gs2LMfuOR9k"
        ),
        Problem(
            id = 43,
            title = "Binary Tree Level Order Traversal",
            category = "Trees",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE"),
            tags = listOf("Tree", "BFS"),
            description = "Return level order traversal of a binary tree's values.",
            examples = listOf(
                Example("root=[3,9,20,null,null,15,7]", "[[3],[9,20],[15,7]]", ""),
            ),
            constraints = listOf("0 <= nodes <= 2000"),
            explanation = "BFS with queue. Process each level by tracking queue size at start of each level.",
            approach = "Queue-based BFS. For each level, process queue.size() nodes, collect values per level.",
            code = "class Solution {\npublic:\n    vector<vector<int>> levelOrder(TreeNode* root) {\n        vector<vector<int>> result;\n        if (!root) return result;\n        queue<TreeNode*> q;\n        q.push(root);\n        while (!q.empty()) {\n            int sz=q.size();\n            vector<int> level;\n            for (int i=0;i<sz;i++) {\n                TreeNode* node=q.front(); q.pop();\n                level.push_back(node->val);\n                if (node->left) q.push(node->left);\n                if (node->right) q.push(node->right);\n            }\n            result.push_back(level);\n        }\n        return result;\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(n)",
            videoUrl = "https://www.youtube.com/watch?v=6ZnyEApgFYg"
        ),
        Problem(
            id = 44,
            title = "Validate Binary Search Tree",
            category = "Trees",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE", "Google Fav"),
            tags = listOf("BST", "DFS"),
            description = "Determine if a binary tree is a valid BST.",
            examples = listOf(
                Example("root=[2,1,3]", "true", ""),
                Example("root=[5,1,4,null,null,3,6]", "false", ""),
            ),
            constraints = listOf("1 <= nodes <= 10^4"),
            explanation = "Each node must be within a valid range. Pass min/max bounds down the tree.",
            approach = "Recursive check with range [min,max]. Left child: update max. Right: update min.",
            code = "class Solution {\npublic:\n    bool isValidBST(TreeNode* root) {\n        return validate(root, LONG_MIN, LONG_MAX);\n    }\n    bool validate(TreeNode* node, long minVal, long maxVal) {\n        if (!node) return true;\n        if (node->val <= minVal || node->val >= maxVal) return false;\n        return validate(node->left,minVal,node->val) &&\n               validate(node->right,node->val,maxVal);\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(h)",
            videoUrl = "https://www.youtube.com/watch?v=s6ATEkipzow"
        ),
        Problem(
            id = 45,
            title = "Kth Smallest Element in BST",
            category = "Trees",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150"),
            tags = listOf("BST", "Inorder"),
            description = "Return the kth smallest value in a BST (1-indexed).",
            examples = listOf(
                Example("root=[3,1,4,null,2], k=1", "1", ""),
            ),
            constraints = listOf("1 <= k <= nodes <= 10^4"),
            explanation = "Inorder traversal of BST gives sorted order. Return the kth element.",
            approach = "Iterative inorder with stack. Count visited nodes. When count equals k, return value.",
            code = "class Solution {\npublic:\n    int kthSmallest(TreeNode* root, int k) {\n        stack<TreeNode*> st;\n        TreeNode* curr = root;\n        while (curr || !st.empty()) {\n            while (curr) { st.push(curr); curr=curr->left; }\n            curr=st.top(); st.pop();\n            if (--k == 0) return curr->val;\n            curr = curr->right;\n        }\n        return -1;\n    }\n};",
            timeComplexity = "O(h+k)",
            spaceComplexity = "O(h)",
            videoUrl = "https://www.youtube.com/watch?v=5LUXSvjmGCw"
        ),
        Problem(
            id = 46,
            title = "Construct Binary Tree from Preorder and Inorder",
            category = "Trees",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE"),
            tags = listOf("Tree", "Recursion", "Hash Map"),
            description = "Construct binary tree given preorder and inorder traversal arrays.",
            examples = listOf(
                Example("preorder=[3,9,20,15,7], inorder=[9,3,15,20,7]", "[3,9,20,null,null,15,7]", ""),
            ),
            constraints = listOf("1 <= length <= 3000"),
            explanation = "First element of preorder is root. Find it in inorder to split left/right subtrees.",
            approach = "Map inorder values to indices. preorder[0] is root. Split inorder at root. Recurse.",
            code = "class Solution {\n    unordered_map<int,int> inMap;\n    int preIdx = 0;\npublic:\n    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {\n        for (int i=0;i<inorder.size();i++) inMap[inorder[i]]=i;\n        return build(preorder, 0, inorder.size()-1);\n    }\n    TreeNode* build(vector<int>& pre, int inL, int inR) {\n        if (inL > inR) return nullptr;\n        int val = pre[preIdx++];\n        TreeNode* root = new TreeNode(val);\n        root->left = build(pre, inL, inMap[val]-1);\n        root->right = build(pre, inMap[val]+1, inR);\n        return root;\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(n)",
            videoUrl = "https://www.youtube.com/watch?v=ihj4IQGZ2zc"
        ),
        Problem(
            id = 47,
            title = "Binary Tree Maximum Path Sum",
            category = "Trees",
            difficulty = "Hard",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE", "Google Fav"),
            tags = listOf("Tree", "DFS"),
            description = "Find maximum path sum in a binary tree. Path can start and end at any node.",
            examples = listOf(
                Example("root=[-10,9,20,null,null,15,7]", "42", "15->20->7"),
            ),
            constraints = listOf("1 <= nodes <= 3*10^4"),
            explanation = "15->20->7",
            approach = "DFS returns max gain from one side. Update global max with node+left+right.",
            code = "class Solution {\n    int maxSum = INT_MIN;\npublic:\n    int maxPathSum(TreeNode* root) { dfs(root); return maxSum; }\n    int dfs(TreeNode* node) {\n        if (!node) return 0;\n        int left = max(0, dfs(node->left));\n        int right = max(0, dfs(node->right));\n        maxSum = max(maxSum, node->val + left + right);\n        return node->val + max(left, right);\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(h)",
            videoUrl = "https://www.youtube.com/watch?v=Hr5cWUld4vU"
        ),
        Problem(
            id = 48,
            title = "Serialize and Deserialize Binary Tree",
            category = "Trees",
            difficulty = "Hard",
            sources = listOf("Blind 75", "Neetcode 150", "Google Fav"),
            tags = listOf("Tree", "Design"),
            description = "Serialize a binary tree to string and deserialize back.",
            examples = listOf(
                Example("root=[1,2,3,null,null,4,5]", "\"1,2,N,N,3,4,N,N,5,N,N", ""),
            ),
            constraints = listOf("0 <= nodes <= 10^4"),
            explanation = "Preorder traversal with 'N' for null. Deserialize by reading tokens in same order.",
            approach = "Serialize: preorder DFS. Deserialize: read tokens, recursively build left then right.",
            code = "class Codec {\npublic:\n    string serialize(TreeNode* root) {\n        if (!root) return \"N\";\n        return to_string(root->val)+\",\"+serialize(root->left)+\",\"+serialize(root->right);\n    }\n    TreeNode* deserialize(string data) {\n        istringstream ss(data);\n        return build(ss);\n    }\n    TreeNode* build(istringstream& ss) {\n        string val; getline(ss, val, ',');\n        if (val == \"N\") return nullptr;\n        TreeNode* node = new TreeNode(stoi(val));\n        node->left = build(ss);\n        node->right = build(ss);\n        return node;\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(n)",
            videoUrl = "https://www.youtube.com/watch?v=u4JAi2JJhI8"
        ),
        Problem(
            id = 49,
            title = "Diameter of Binary Tree",
            category = "Trees",
            difficulty = "Easy",
            sources = listOf("Neetcode 150", "Striver SDE"),
            tags = listOf("Tree", "DFS"),
            description = "Return diameter of binary tree (longest path between any two nodes in edges).",
            examples = listOf(
                Example("root=[1,2,3,4,5]", "3", ""),
            ),
            constraints = listOf("1 <= nodes <= 10^4"),
            explanation = "At each node, diameter through it = leftHeight + rightHeight. Track global max.",
            approach = "DFS returns height. Update diameter = max(diameter, left+right). Return 1+max(left,right).",
            code = "class Solution {\n    int dia = 0;\npublic:\n    int diameterOfBinaryTree(TreeNode* root) { height(root); return dia; }\n    int height(TreeNode* node) {\n        if (!node) return 0;\n        int l=height(node->left), r=height(node->right);\n        dia = max(dia, l+r);\n        return 1 + max(l, r);\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(h)",
            videoUrl = "https://www.youtube.com/watch?v=bkxqA8Rfv04"
        ),
        Problem(
            id = 50,
            title = "Balanced Binary Tree",
            category = "Trees",
            difficulty = "Easy",
            sources = listOf("Neetcode 150"),
            tags = listOf("Tree", "DFS"),
            description = "Determine if a binary tree is height-balanced.",
            examples = listOf(
                Example("root=[3,9,20,null,null,15,7]", "true", ""),
            ),
            constraints = listOf("0 <= nodes <= 5000"),
            explanation = "Check if left and right subtree heights differ by at most 1. Return -1 if unbalanced.",
            approach = "DFS returns height or -1 if unbalanced. If |left-right| > 1, return -1.",
            code = "class Solution {\npublic:\n    bool isBalanced(TreeNode* root) { return check(root) != -1; }\n    int check(TreeNode* node) {\n        if (!node) return 0;\n        int l=check(node->left); if (l==-1) return -1;\n        int r=check(node->right); if (r==-1) return -1;\n        if (abs(l-r)>1) return -1;\n        return 1+max(l,r);\n    }\n};",
            timeComplexity = "O(n)",
            spaceComplexity = "O(h)",
            videoUrl = "https://www.youtube.com/watch?v=QfJsau0ItOY"
        ),
        Problem(
            id = 51,
            title = "Number of Islands",
            category = "Graphs",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE", "Google Fav"),
            tags = listOf("BFS", "DFS", "Matrix"),
            description = "Given a 2D grid of '1's (land) and '0's (water), count the number of islands.",
            examples = listOf(
                Example("grid = [[\"1", "2", ""),
            ),
            constraints = listOf("1 <= m, n <= 300"),
            explanation = "For each unvisited '1', run DFS/BFS to mark all connected land. Each DFS call = one island.",
            approach = "Iterate grid. When '1' found, increment count, DFS to mark all connected '1's as visited.",
            code = "class Solution {\npublic:\n    int numIslands(vector<vector<char>>& grid) {\n        int count = 0, m = grid.size(), n = grid[0].size();\n        for (int i = 0; i < m; i++)\n            for (int j = 0; j < n; j++)\n                if (grid[i][j] == '1') { count++; dfs(grid, i, j); }\n        return count;\n    }\n    void dfs(vector<vector<char>>& g, int i, int j) {\n        if (i<0||j<0||i>=g.size()||j>=g[0].size()||g[i][j]!='1') return;\n        g[i][j] = '0';\n        dfs(g,i+1,j); dfs(g,i-1,j); dfs(g,i,j+1); dfs(g,i,j-1);\n    }\n};",
            timeComplexity = "O(m*n)",
            spaceComplexity = "O(m*n)",
            videoUrl = "https://www.youtube.com/watch?v=pV2kpPD66nE"
        ),
        Problem(
            id = 52,
            title = "Clone Graph",
            category = "Graphs",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150"),
            tags = listOf("BFS", "DFS", "Hash Map"),
            description = "Return a deep copy of a connected undirected graph.",
            examples = listOf(
                Example("adjList = [[2,4],[1,3],[2,4],[1,3]]", "deep copy", ""),
            ),
            constraints = listOf("0 <= nodes <= 100", "No repeated edges or self-loops"),
            explanation = "Use a hash map from old node to cloned node. DFS/BFS and clone neighbors recursively.",
            approach = "Map old->new. For each node, create clone, recursively clone neighbors if not already cloned.",
            code = "class Solution {\n    unordered_map<Node*, Node*> visited;\npublic:\n    Node* cloneGraph(Node* node) {\n        if (!node) return nullptr;\n        if (visited.count(node)) return visited[node];\n        Node* clone = new Node(node->val);\n        visited[node] = clone;\n        for (auto* nb : node->neighbors)\n            clone->neighbors.push_back(cloneGraph(nb));\n        return clone;\n    }\n};",
            timeComplexity = "O(V+E)",
            spaceComplexity = "O(V)",
            videoUrl = "https://www.youtube.com/watch?v=mQeF6bN8hMk"
        ),
        Problem(
            id = 53,
            title = "Pacific Atlantic Water Flow",
            category = "Graphs",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150"),
            tags = listOf("BFS", "DFS", "Matrix"),
            description = "Find all cells where water can flow to both Pacific and Atlantic oceans.",
            examples = listOf(
                Example("heights = [[1,2,2,3,5],[3,2,3,4,4],[2,4,5,3,1],[6,7,1,4,5],[5,1,1,2,4]]", "[[0,4],[1,3],[1,4],[2,2],[3,0],[3,1],[4,0]]", ""),
            ),
            constraints = listOf("1 <= m, n <= 200"),
            explanation = "BFS/DFS from ocean borders inward. Find cells reachable from both oceans.",
            approach = "DFS from Pacific border and Atlantic border separately. Return intersection.",
            code = "class Solution {\npublic:\n    vector<vector<int>> pacificAtlantic(vector<vector<int>>& heights) {\n        int m=heights.size(), n=heights[0].size();\n        vector<vector<bool>> pac(m,vector<bool>(n)), atl(m,vector<bool>(n));\n        for (int i=0;i<m;i++) { dfs(heights,pac,i,0); dfs(heights,atl,i,n-1); }\n        for (int j=0;j<n;j++) { dfs(heights,pac,0,j); dfs(heights,atl,m-1,j); }\n        vector<vector<int>> res;\n        for (int i=0;i<m;i++)\n            for (int j=0;j<n;j++)\n                if (pac[i][j]&&atl[i][j]) res.push_back({i,j});\n        return res;\n    }\n    void dfs(vector<vector<int>>& h, vector<vector<bool>>& v, int i, int j) {\n        v[i][j]=true;\n        int d[]={0,1,0,-1,0};\n        for (int k=0;k<4;k++) {\n            int ni=i+d[k],nj=j+d[k+1];\n            if (ni>=0&&nj>=0&&ni<h.size()&&nj<h[0].size()&&!v[ni][nj]&&h[ni][nj]>=h[i][j])\n                dfs(h,v,ni,nj);\n        }\n    }\n};",
            timeComplexity = "O(m*n)",
            spaceComplexity = "O(m*n)",
            videoUrl = "https://www.youtube.com/watch?v=s-VkcjHqkGI"
        ),
        Problem(
            id = 54,
            title = "Course Schedule",
            category = "Graphs",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE", "Google Fav"),
            tags = listOf("Topological Sort", "BFS", "DFS"),
            description = "Determine if you can finish all courses given prerequisites (detect cycle in directed graph).",
            examples = listOf(
                Example("numCourses=2, prerequisites=[[1,0]]", "true", ""),
                Example("numCourses=2, prerequisites=[[1,0],[0,1]]", "false", ""),
            ),
            constraints = listOf("1 <= numCourses <= 2000"),
            explanation = "This is cycle detection in a directed graph. Use DFS with 3 states or BFS topological sort (Kahn's).",
            approach = "Kahn's algorithm: count in-degrees, BFS from nodes with 0 in-degree. If all processed, no cycle.",
            code = "class Solution {\npublic:\n    bool canFinish(int n, vector<vector<int>>& prerequisites) {\n        vector<vector<int>> adj(n);\n        vector<int> indeg(n, 0);\n        for (auto& p : prerequisites) { adj[p[1]].push_back(p[0]); indeg[p[0]]++; }\n        queue<int> q;\n        for (int i=0;i<n;i++) if (indeg[i]==0) q.push(i);\n        int count = 0;\n        while (!q.empty()) {\n            int node=q.front(); q.pop(); count++;\n            for (int nb : adj[node]) if (--indeg[nb]==0) q.push(nb);\n        }\n        return count == n;\n    }\n};",
            timeComplexity = "O(V+E)",
            spaceComplexity = "O(V+E)",
            videoUrl = "https://www.youtube.com/watch?v=EgI5nU9etnU"
        ),
        Problem(
            id = 55,
            title = "Course Schedule II",
            category = "Graphs",
            difficulty = "Medium",
            sources = listOf("Neetcode 150", "Striver SDE"),
            tags = listOf("Topological Sort", "BFS"),
            description = "Return the ordering of courses you should take to finish all courses (topological sort).",
            examples = listOf(
                Example("numCourses=4, prerequisites=[[1,0],[2,0],[3,1],[3,2]]", "[0,2,1,3]", ""),
            ),
            constraints = listOf("1 <= numCourses <= 2000"),
            explanation = "Topological sort using Kahn's algorithm. If cycle exists, return empty array.",
            approach = "Same as Course Schedule but collect the order. Return empty if count != numCourses.",
            code = "class Solution {\npublic:\n    vector<int> findOrder(int n, vector<vector<int>>& prerequisites) {\n        vector<vector<int>> adj(n);\n        vector<int> indeg(n,0);\n        for (auto& p : prerequisites) { adj[p[1]].push_back(p[0]); indeg[p[0]]++; }\n        queue<int> q;\n        for (int i=0;i<n;i++) if (indeg[i]==0) q.push(i);\n        vector<int> order;\n        while (!q.empty()) {\n            int node=q.front(); q.pop(); order.push_back(node);\n            for (int nb : adj[node]) if (--indeg[nb]==0) q.push(nb);\n        }\n        return order.size()==n ? order : vector<int>{};\n    }\n};",
            timeComplexity = "O(V+E)",
            spaceComplexity = "O(V+E)",
            videoUrl = "https://www.youtube.com/watch?v=Akt3glAwyfY"
        ),
        Problem(
            id = 56,
            title = "Graph Valid Tree",
            category = "Graphs",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150"),
            tags = listOf("Union Find", "DFS"),
            description = "Given n nodes and edges, determine if these edges form a valid tree (connected, no cycles).",
            examples = listOf(
                Example("n=5, edges=[[0,1],[0,2],[0,3],[1,4]]", "true", ""),
            ),
            constraints = listOf("1 <= n <= 2000"),
            explanation = "A valid tree has exactly n-1 edges and is connected. Use Union-Find to detect cycles.",
            approach = "Check edges == n-1. Use Union-Find. If any edge connects already-connected nodes, it's a cycle.",
            code = "class Solution {\n    vector<int> parent, rank_;\npublic:\n    bool validTree(int n, vector<vector<int>>& edges) {\n        if (edges.size() != n-1) return false;\n        parent.resize(n); rank_.resize(n, 0);\n        iota(parent.begin(), parent.end(), 0);\n        for (auto& e : edges)\n            if (!unite(e[0], e[1])) return false;\n        return true;\n    }\n    int find(int x) { return parent[x]==x ? x : parent[x]=find(parent[x]); }\n    bool unite(int a, int b) {\n        a=find(a); b=find(b);\n        if (a==b) return false;\n        if (rank_[a]<rank_[b]) swap(a,b);\n        parent[b]=a;\n        if (rank_[a]==rank_[b]) rank_[a]++;\n        return true;\n    }\n};",
            timeComplexity = "O(n * α(n))",
            spaceComplexity = "O(n)",
            videoUrl = "https://www.youtube.com/watch?v=bXsUuownnoQ"
        ),
        Problem(
            id = 57,
            title = "Number of Connected Components",
            category = "Graphs",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150"),
            tags = listOf("Union Find", "DFS"),
            description = "Find the number of connected components in an undirected graph with n nodes.",
            examples = listOf(
                Example("n=5, edges=[[0,1],[1,2],[3,4]]", "2", ""),
            ),
            constraints = listOf("1 <= n <= 2000"),
            explanation = "Use Union-Find. Initially n components. Each successful union reduces count by 1.",
            approach = "Union-Find with path compression. Start with n components, decrement on each union.",
            code = "class Solution {\n    vector<int> parent;\npublic:\n    int countComponents(int n, vector<vector<int>>& edges) {\n        parent.resize(n);\n        iota(parent.begin(), parent.end(), 0);\n        int count = n;\n        for (auto& e : edges) {\n            int a=find(e[0]), b=find(e[1]);\n            if (a!=b) { parent[a]=b; count--; }\n        }\n        return count;\n    }\n    int find(int x) { return parent[x]==x ? x : parent[x]=find(parent[x]); }\n};",
            timeComplexity = "O(n * α(n))",
            spaceComplexity = "O(n)",
            videoUrl = "https://www.youtube.com/watch?v=8f1XPm4WOUc"
        ),
        Problem(
            id = 58,
            title = "Word Ladder",
            category = "Graphs",
            difficulty = "Hard",
            sources = listOf("Neetcode 150", "Striver SDE", "Google Fav"),
            tags = listOf("BFS", "Hash Set"),
            description = "Find shortest transformation sequence length from beginWord to endWord, changing one letter at a time.",
            examples = listOf(
                Example("beginWord=\"hit", "5", ""),
            ),
            constraints = listOf("1 <= beginWord.length <= 10", "1 <= wordList.length <= 5000"),
            explanation = "BFS from beginWord. Each level, try all single-char changes. First time we reach endWord is shortest path.",
            approach = "BFS with word set. For each word, try all 26 chars at each position. If in set, add to queue.",
            code = "class Solution {\npublic:\n    int ladderLength(string beginWord, string endWord, vector<string>& wordList) {\n        unordered_set<string> words(wordList.begin(), wordList.end());\n        if (!words.count(endWord)) return 0;\n        queue<string> q;\n        q.push(beginWord);\n        int steps = 1;\n        while (!q.empty()) {\n            int sz = q.size();\n            for (int i=0;i<sz;i++) {\n                string word = q.front(); q.pop();\n                for (int j=0;j<word.size();j++) {\n                    char orig = word[j];\n                    for (char c='a';c<='z';c++) {\n                        word[j] = c;\n                        if (word == endWord) return steps+1;\n                        if (words.count(word)) { words.erase(word); q.push(word); }\n                    }\n                    word[j] = orig;\n                }\n            }\n            steps++;\n        }\n        return 0;\n    }\n};",
            timeComplexity = "O(n * m * 26)",
            spaceComplexity = "O(n * m)",
            videoUrl = "https://www.youtube.com/watch?v=h9iTnkgv05E"
        ),
        Problem(
            id = 59,
            title = "Kth Largest Element in Array",
            category = "Heap",
            difficulty = "Medium",
            sources = listOf("Blind 75", "Neetcode 150", "Striver SDE", "Google Fav"),
            tags = listOf("Heap", "QuickSelect"),
            description = "Find the kth largest element in an unsorted array.",
            examples = listOf(
                Example("nums=[3,2,1,5,6,4], k=2", "5", ""),
            ),
            constraints = listOf("1 <= k <= nums.length <= 10^5"),
            explanation = "Use a min-heap of size k. The top of heap is the kth largest element.",
            approach = "Push elements to min-heap. If size > k, pop. Final top = kth largest.",
            code = "class Solution {\npublic:\n    int findKthLargest(vector<int>& nums, int k) {\n        priority_queue<int, vector<int>, greater<int>> pq;\n        for (int n : nums) {\n            pq.push(n);\n            if (pq.size() > k) pq.pop();\n        }\n        return pq.top();\n    }\n};",
            timeComplexity = "O(n log k)",
            spaceComplexity = "O(k)",
            videoUrl = "https://www.youtube.com/watch?v=XEmy13g1Qxc"
        ),
        Problem(
            id = 60,
            title = "Find Median from Data Stream",
            category = "Heap",
            difficulty = "Hard",
            sources = listOf("Blind 75", "Neetcode 150", "Google Fav"),
            tags = listOf("Heap", "Design"),
            description = "Design a data structure that supports adding numbers and finding the median.",
            examples = listOf(
                Example("addNum(1), addNum(2), findMedian() -> 1.5, addNum(3), findMedian() -> 2.0", "1.5, 2.0", ""),
            ),
            constraints = listOf("At most 5*10^4 calls"),
            explanation = "Use two heaps: max-heap for lower half, min-heap for upper half. Median is from tops of heaps.",
            approach = "Max-heap stores smaller half, min-heap stores larger half. Balance sizes. Median from top(s).",
            code = "class MedianFinder {\n    priority_queue<int> lo; // max-heap\n    priority_queue<int, vector<int>, greater<int>> hi; // min-heap\npublic:\n    void addNum(int num) {\n        lo.push(num);\n        hi.push(lo.top()); lo.pop();\n        if (hi.size() > lo.size()) { lo.push(hi.top()); hi.pop(); }\n    }\n    double findMedian() {\n        return lo.size() > hi.size() ? lo.top() : (lo.top()+hi.top())/2.0;\n    }\n};",
            timeComplexity = "O(log n) add, O(1) find",
            spaceComplexity = "O(n)",
            videoUrl = "https://www.youtube.com/watch?v=itmhHWaHupI"
        )
    )
